import { Theme } from '@emotion/react';

const theme: Theme = {
    color: {
        primary: 'olive',
    },

    screen: {
        fold: false,
        mobile: false,
        desktop: false,
    },
};

export default theme;
